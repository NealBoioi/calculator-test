# calculator-test

A simple static calculator app you can open from VS Code.

## Run the calculator

1. Open `index.html` in VS Code.
2. Use the Live Server extension or open the file in your browser.
3. Click the buttons to perform calculations.

## Files

- `index.html` — calculator page
- `styles.css` — styling
- `script.js` — calculator logic
